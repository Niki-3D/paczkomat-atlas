export default function LandingPage() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center space-y-4">
        <h1 className="text-2xl font-semibold">Paczkomat Atlas</h1>
        <p className="text-sm text-[var(--fg-muted)]">Landing page — TODO</p>
        <a href="/app" className="text-[var(--accent)] underline underline-offset-4">
          Open dashboard →
        </a>
      </div>
    </main>
  );
}
